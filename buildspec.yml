version: 0.2

phases:
  install:
    commands:
      - apt update
      - apt-get install openjdk-11-jdk -y
      - java -version
      - wget https://services.gradle.org/distributions/gradle-6.6-bin.zip -P /tmp
      - unzip -d /opt/gradle /tmp/gradle-*.zip
      - ls /opt/gradle/gradle-6.6
      - export GRADLE_HOME=/opt/gradle/gradle-6.6
      - export PATH=${GRADLE_HOME}/bin:${PATH}
      - gradle -v

  build:
    commands:
      - cd location-crud
      - gradle buildZip
      - cd ..
      - cd product-crud
      - gradle buildZip
      - cd ..
      - cd balance-crud
      - gradle buildZip
      - cd ..
artifacts:
  secondary-artifacts:
    location_crud:
      files:
        - location-crud/build/distributions/location-crud-0.0.1-SNAPSHOT.zip
      discard-paths: yes
      name: location_crud
    product_crud:
      files:
        - product-crud/build/distributions/product-crud-0.0.1-SNAPSHOT.zip
      discard-paths: yes
      name: product_crud
    balance_crud:
      files:
        - balance-crud/build/distributions/balance-crud-0.0.1-SNAPSHOT.zip
      discard-paths: yes
      name: balance_crud